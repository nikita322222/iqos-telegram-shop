# 🔧 Решение проблем

## Товары не отображаются в Mini App

### Проблема
При открытии магазина через Telegram бота товары не загружаются.

### Причины и решения

#### 1. Backend еще деплоится на Render
Render может деплоить изменения 2-5 минут после push в GitHub.

**Проверка:**
```bash
curl "https://iqos-backend.onrender.com/api/products?limit=1"
```

Если возвращается ошибка авторизации - backend еще не обновился.

**Решение:** Подождите 5 минут и проверьте снова.

#### 2. CORS не настроен
Frontend не может обращаться к backend из-за CORS политики.

**Решение:**
1. Зайдите на [Render Dashboard](https://dashboard.render.com)
2. Выберите ваш backend сервис
3. Environment → Add Environment Variable
4. Добавьте:
   ```
   CORS_ORIGINS=https://iqos-shop.vercel.app
   ```
5. Сохраните (сервис автоматически перезапустится)

#### 3. Проблема с авторизацией Telegram
Telegram initData не проходит валидацию.

**Временное решение:**
Мы сделали API `/api/products` публичным (без авторизации), чтобы товары загружались всегда.

**Постоянное решение:**
Убедитесь, что:
- BOT_TOKEN одинаковый в bot и backend
- Menu Button настроен правильно в BotFather
- Mini App открывается через кнопку в боте, а не напрямую

#### 4. База данных пустая на продакшне
Товары не загружены на продакшн backend.

**Проверка:**
```bash
curl "https://iqos-backend.onrender.com/api/admin/stats"
```

Если `products: 0` - товары не загружены.

**Решение:**
```bash
cd backend
python3 upload_to_production.py
```

## Локальное тестирование

### Запуск всех сервисов

```bash
# Terminal 1: Backend
cd backend
python3 main.py

# Terminal 2: Frontend  
cd frontend
npm run dev

# Terminal 3: Bot
cd bot
python3 main.py
```

### Проверка работы

1. **Backend**: http://localhost:8000
   ```bash
   curl "http://localhost:8000/api/products?limit=1"
   ```

2. **Frontend**: http://localhost:5173
   Откройте в браузере

3. **Bot**: @eqweqweqweeeqw_bot
   Отправьте `/start` в Telegram

### Тестирование без Telegram

Откройте http://localhost:5173 в браузере. Товары должны загружаться даже без Telegram Web App.

## Проверка деплоя на Render

### Логи backend
1. Зайдите на [Render Dashboard](https://dashboard.render.com)
2. Выберите ваш backend сервис
3. Logs → посмотрите последние логи

### Ручной редеплой
Если автодеплой не сработал:
1. Render Dashboard → ваш сервис
2. Manual Deploy → Deploy latest commit

## Проверка деплоя на Vercel

### Статус деплоя
1. Зайдите на [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект iqos-shop
3. Deployments → посмотрите статус последнего деплоя

### Логи
Кликните на последний деплой → Build Logs

## Частые ошибки

### "Authorization header отсутствует"
**Причина:** Старая версия backend на продакшне  
**Решение:** Подождите завершения деплоя на Render

### "CORS policy error"
**Причина:** CORS_ORIGINS не настроен  
**Решение:** Добавьте переменную окружения на Render

### "Товары не найдены"
**Причина:** База данных пустая  
**Решение:** Запустите `python3 upload_to_production.py`

### "Failed to fetch"
**Причина:** Backend недоступен  
**Решение:** Проверьте, что backend запущен и доступен

## Полезные команды

```bash
# Проверка статуса продакшн backend
curl https://iqos-backend.onrender.com/api/admin/stats

# Проверка товаров
curl "https://iqos-backend.onrender.com/api/products?limit=5"

# Проверка frontend
curl -I https://iqos-shop.vercel.app

# Загрузка товаров на продакшн
cd backend && python3 upload_to_production.py

# Проверка локального backend
curl http://localhost:8000/api/admin/stats

# Проверка локальных товаров
curl "http://localhost:8000/api/products?limit=5"
```

## Контакты для поддержки

Если проблема не решается:
1. Проверьте логи на Render
2. Проверьте логи на Vercel
3. Проверьте переменные окружения
4. Убедитесь, что все сервисы запущены

---

**Последнее обновление:** 13 января 2026
