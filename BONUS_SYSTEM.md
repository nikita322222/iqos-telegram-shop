# 🎁 Бонусная система лояльности

## Обзор

Реализована трехуровневая система лояльности с кэшбэком для постоянных клиентов.

## 🏆 Уровни лояльности

### 🥉 Bronze (Бронза)
- **Условие**: 0-5 завершенных заказов
- **Кэшбэк**: 1.5% от суммы заказа
- **Начальный уровень** для всех новых пользователей

### 🥈 Silver (Серебро)
- **Условие**: 6-15 завершенных заказов
- **Кэшбэк**: 3% от суммы заказа
- **Повышенный кэшбэк** для активных покупателей

### 🥇 Gold (Золото)
- **Условие**: 16+ завершенных заказов
- **Кэшбэк**: 5% от суммы заказа
- **Максимальный уровень** для VIP клиентов

## 💰 Как работает система

### Начисление бонусов
1. Клиент оформляет заказ
2. Админ подтверждает заказ (меняет статус на "confirmed")
3. Автоматически начисляются бонусы по формуле:
   ```
   Бонусы = (Сумма заказа + Использованные бонусы) × Процент кэшбэка
   ```
4. Обновляется счетчик заказов
5. Проверяется и обновляется уровень лояльности

### Использование бонусов
1. При оформлении заказа клиент видит свой баланс бонусов
2. Может включить чекбокс "Использовать бонусы"
3. Указывает сумму бонусов для списания (до 100% от суммы заказа)
4. Бонусы списываются сразу при создании заказа
5. **1 бонус = 1 рубль**

### Повышение уровня
- Уровень автоматически повышается при достижении нужного количества заказов
- Повышение происходит при подтверждении заказа админом
- Новый процент кэшбэка применяется к следующим заказам

## 📱 Интерфейс

### Страница "Бонусы" (⭐ в нижнем меню)

**Отображается:**
- Текущий баланс бонусов
- Уровень лояльности с иконкой
- Процент кэшбэка
- Прогресс-бар до следующего уровня
- Информация о системе
- Описание всех уровней
- История начислений и списаний

**Дизайн:**
- Градиентная карточка с балансом
- Цветная рамка карточки уровня (бронза/серебро/золото)
- Анимированный прогресс-бар
- Информационные карточки с иконками
- История транзакций с датами

### Оформление заказа

**Если есть бонусы:**
- Показывается секция "💰 Использовать бонусы"
- Отображается доступный баланс
- Чекбокс для активации использования
- Поле ввода суммы бонусов
- Подсказка с максимальной суммой
- Итоговый расчет с учетом бонусов

## 🗄️ База данных

### Таблица `users`
Добавлены поля:
- `bonus_balance` (FLOAT) - текущий баланс бонусов
- `total_orders_count` (INTEGER) - количество завершенных заказов
- `loyalty_level` (VARCHAR) - уровень лояльности (bronze/silver/gold)

### Таблица `orders`
Добавлены поля:
- `bonus_used` (FLOAT) - сколько бонусов использовано в заказе
- `bonus_earned` (FLOAT) - сколько бонусов начислено за заказ

### Таблица `bonus_transactions`
Новая таблица для истории:
- `id` - ID транзакции
- `user_id` - ID пользователя
- `amount` - сумма (+ начисление, - списание)
- `transaction_type` - тип (earned/spent/expired)
- `description` - описание операции
- `order_id` - связь с заказом
- `created_at` - дата и время

## 🔧 API Endpoints

### `GET /api/bonus/info`
Получение информации о бонусах пользователя

**Response:**
```json
{
  "bonus_balance": 150.50,
  "loyalty_level": "silver",
  "total_orders_count": 8,
  "cashback_percent": 3.0,
  "next_level_orders": 16,
  "progress_percent": 20.0
}
```

### `GET /api/bonus/transactions`
История бонусных транзакций (последние 20)

**Response:**
```json
[
  {
    "id": 1,
    "amount": 45.50,
    "transaction_type": "earned",
    "description": "Начислено за заказ #123 (3% кэшбэк)",
    "order_id": 123,
    "created_at": "2025-01-16T10:30:00"
  }
]
```

### `POST /api/orders`
Создание заказа (обновлено)

**Request добавлено поле:**
```json
{
  "bonus_to_use": 50.00,
  ...
}
```

### `PATCH /api/orders/{order_id}/status`
Обновление статуса заказа (обновлено)

При смене статуса с `pending` на `confirmed`:
- Начисляются бонусы
- Увеличивается счетчик заказов
- Обновляется уровень лояльности
- Создается транзакция начисления

## 📊 Примеры расчетов

### Пример 1: Bronze уровень
- Сумма заказа: 1000 BYN
- Кэшбэк: 1.5%
- Начислено: **15 BYN**

### Пример 2: Silver уровень
- Сумма заказа: 1500 BYN
- Использовано бонусов: 200 BYN
- К оплате: 1300 BYN
- Кэшбэк: 3% от (1500 + 200) = 3% от 1700
- Начислено: **51 BYN**

### Пример 3: Gold уровень
- Сумма заказа: 2000 BYN
- Кэшбэк: 5%
- Начислено: **100 BYN**

## 🚀 Миграция

Для добавления бонусной системы в существующую базу:

```bash
cd backend
python3 migrate_bonus_system.py
```

Миграция:
1. Добавляет поля в таблицу `users`
2. Добавляет поля в таблицу `orders`
3. Создает таблицу `bonus_transactions`
4. Обновляет счетчики заказов для существующих пользователей
5. Устанавливает уровни лояльности на основе истории

## 📝 Логика работы

### При создании заказа:
```python
# Списываем бонусы если указаны
if bonus_to_use > 0:
    user.bonus_balance -= bonus_to_use
    # Создаем транзакцию списания
```

### При подтверждении заказа:
```python
# Вычисляем кэшбэк
cashback_percent = get_cashback_percent(user.loyalty_level)
bonus_earned = (order.total_amount + order.bonus_used) * cashback_percent / 100

# Начисляем бонусы
user.bonus_balance += bonus_earned
user.total_orders_count += 1

# Обновляем уровень
new_level = calculate_loyalty_level(user.total_orders_count)
if new_level != user.loyalty_level:
    user.loyalty_level = new_level

# Создаем транзакцию начисления
```

## 🎨 Дизайн особенности

- **Градиенты**: Фиолетовый градиент для карточки баланса
- **Цвета уровней**: 
  - Bronze: #CD7F32
  - Silver: #C0C0C0
  - Gold: #FFD700
- **Анимации**: Плавные переходы прогресс-бара
- **Темная тема**: Полная поддержка темной темы
- **Адаптивность**: Оптимизировано для мобильных устройств

## ✅ Преимущества системы

1. **Мотивация**: Клиенты стремятся повысить уровень
2. **Удержание**: Накопленные бонусы удерживают клиентов
3. **Повторные покупки**: Бонусы стимулируют новые заказы
4. **Прозрачность**: Вся история операций доступна
5. **Автоматизация**: Все процессы автоматические
6. **Гибкость**: Легко изменить проценты и условия

## 🔮 Возможные улучшения

- Срок действия бонусов (например, 12 месяцев)
- Бонусы за регистрацию
- Бонусы за приглашение друзей
- Специальные акции с повышенным кэшбэком
- Push-уведомления о начислении бонусов
- Ограничение использования бонусов (например, до 50% от суммы)
