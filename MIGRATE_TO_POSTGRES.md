# 🗄️ Миграция на PostgreSQL

## Проблема
Render использует эфемерную файловую систему. При каждом перезапуске сервиса SQLite база данных очищается и товары пропадают.

## Решение
Мигрировать на PostgreSQL - постоянную базу данных на Render.

## Шаги миграции

### 1. Создайте PostgreSQL базу на Render

1. Зайдите на https://dashboard.render.com
2. Нажмите **New** → **PostgreSQL**
3. Настройки:
   - Name: `iqos-shop-db`
   - Database: `iqos_shop`
   - User: `iqos_shop_user`
   - Region: выберите ближайший
   - Plan: **Free** (для начала)
4. Нажмите **Create Database**
5. Подождите 2-3 минуты пока создастся

### 2. Получите Database URL

После создания БД:
1. Откройте созданную базу данных
2. Найдите **Internal Database URL**
3. Скопируйте (выглядит примерно так):
   ```
   postgresql://user:password@hostname/database
   ```

### 3. Обновите переменные окружения Backend

1. Откройте ваш backend сервис на Render
2. Environment → Add Environment Variable
3. Обновите `DATABASE_URL`:
   ```
   DATABASE_URL=postgresql://user:password@hostname/database
   ```
   (вставьте Internal Database URL из шага 2)
4. Сохраните

Render автоматически перезапустит сервис.

### 4. Загрузите товары

После перезапуска backend:

```bash
cd backend
python3 upload_to_production.py
```

### 5. Проверка

```bash
curl https://iqos-backend.onrender.com/api/admin/stats
```

Должно показать 118 товаров.

## Преимущества PostgreSQL

✅ **Постоянное хранение** - данные не пропадают при перезапуске
✅ **Надежность** - автоматические бэкапы
✅ **Производительность** - лучше для продакшена
✅ **Масштабируемость** - можно увеличить размер БД

## Альтернатива: Автоматическая загрузка товаров

Если не хотите мигрировать на PostgreSQL прямо сейчас, можно настроить автоматическую загрузку товаров при старте backend.

Обновите `backend/main.py`:

```python
@app.on_event("startup")
def startup_event():
    """Инициализация БД при старте"""
    init_db()
    
    from database import SessionLocal
    import models
    
    db = SessionLocal()
    try:
        # Проверяем есть ли товары
        product_count = db.query(models.Product).count()
        
        if product_count == 0:
            print("⚠️  База пустая, загружаем товары...")
            # Здесь можно добавить автоматическую загрузку
            # из offers.xlsx или из бэкапа
    finally:
        db.close()
```

## Текущее решение (временное)

Пока используется SQLite, после каждого перезапуска Render нужно:

```bash
cd backend
python3 upload_to_production.py
```

Это загрузит товары обратно.

## Рекомендация

**Мигрируйте на PostgreSQL как можно скорее** - это займет 10 минут, но избавит от проблем с пропаданием данных.

---

**Статус:** Товары загружены (118 шт)  
**Следующий шаг:** Миграция на PostgreSQL
