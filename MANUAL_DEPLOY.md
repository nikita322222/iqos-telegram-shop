# 🚀 Ручной деплой на Vercel

## Проблема
Автодеплой на Vercel не работает более часа.

## Решение 1: Ручной деплой через Vercel Dashboard

1. Зайдите на https://vercel.com/dashboard
2. Найдите проект `iqos-shop`
3. Перейдите в раздел **Deployments**
4. Нажмите **Redeploy** на последнем деплое
5. Или нажмите **Deploy** → выберите branch `main`

## Решение 2: Деплой через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Перейдите в папку frontend
cd frontend

# Залогиньтесь в Vercel
vercel login

# Задеплойте
vercel --prod
```

## Решение 3: Проверка автодеплоя

1. Зайдите на https://vercel.com/dashboard
2. Выберите проект `iqos-shop`
3. Settings → Git
4. Убедитесь, что:
   - ✅ Production Branch: `main`
   - ✅ Auto Deploy: включен
   - ✅ GitHub репозиторий подключен

## Решение 4: Локальное тестирование

Пока Vercel деплоится, можно протестировать локально:

```bash
# Запустите локальный frontend
cd frontend
npm run dev

# Откройте в браузере
# http://localhost:5173
```

Проверьте скролл в браузере. Если работает локально, значит код правильный.

## Проверка деплоя

После деплоя проверьте:

```bash
# Проверка HTML
curl https://iqos-shop.vercel.app | grep "overflow-y: scroll"

# Проверка CSS хэша (должен измениться)
curl https://iqos-shop.vercel.app | grep "index-.*\.css"
```

Новый CSS файл должен называться `index-C-ZWtSnP.css` (не `index-BMp9EmLj.css`).

## Текущий статус

- ✅ Локальный билд создан: `dist/assets/index-C-ZWtSnP.css`
- ❌ Vercel не обновился: все еще использует `index-BMp9EmLj.css`
- ✅ Код правильный: скролл должен работать после деплоя

## Что делать дальше

1. Попробуйте ручной деплой через Dashboard
2. Если не помогает - используйте Vercel CLI
3. Проверьте логи деплоя на Vercel
4. Убедитесь, что нет ошибок в билде

---

**Важно:** После успешного деплоя очистите кэш в Telegram:
Settings → Data and Storage → Clear Cache
