# Как добавить пользователей в базу данных

## Какие данные нужны

### Обязательно:
- **Telegram ID** - уникальный ID пользователя в Telegram

### Опционально:
- Username (без @)
- Имя
- Фамилия

## Как узнать Telegram ID пользователя

### Способ 1: Через бота @userinfobot
1. Пользователь открывает бота @userinfobot
2. Отправляет любое сообщение
3. Бот показывает его Telegram ID

### Способ 2: Через бота @getmyid_bot
1. Пользователь открывает бота @getmyid_bot
2. Нажимает /start
3. Бот показывает его ID

### Способ 3: Через ваш бот (если добавить функцию)
Пользователь может написать /start вашему боту, и бот покажет его ID в логах

## Добавление пользователей

### Вариант 1: Интерактивный режим (рекомендуется)

```bash
cd backend
python3 add_user.py
```

Появится меню:
```
1. Добавить пользователя
2. Показать всех пользователей
3. Деактивировать пользователя
4. Выход
```

Выбери **1** и введи данные:
- Telegram ID (обязательно)
- Username (опционально)
- Имя (опционально)
- Фамилия (опционально)

### Вариант 2: Быстрое добавление через командную строку

```bash
cd backend
python3 add_user.py TELEGRAM_ID [username] [имя] [фамилия]
```

**Примеры:**

Только Telegram ID:
```bash
python3 add_user.py 123456789
```

С username:
```bash
python3 add_user.py 123456789 ivan_petrov
```

С полными данными:
```bash
python3 add_user.py 123456789 ivan_petrov "Иван" "Петров"
```

### Вариант 3: Добавление в продакшн базу

Открой файл `backend/add_user.py` и раскомментируй строку:
```python
os.environ['DATABASE_URL'] = "postgresql://..."
```

Затем запусти скрипт как обычно.

## Просмотр всех пользователей

```bash
cd backend
python3 add_user.py
```

Выбери **2** в меню.

Или используй Python:
```bash
cd backend
python3 -c "from add_user import list_users; list_users()"
```

## Деактивация пользователя

Если нужно заблокировать доступ пользователю (не удаляя его из базы):

```bash
cd backend
python3 add_user.py
```

Выбери **3** и введи Telegram ID пользователя.

## Пример: Добавление нескольких пользователей

```bash
cd backend

# Добавляем первого пользователя
python3 add_user.py 123456789 user1 "Иван" "Иванов"

# Добавляем второго пользователя
python3 add_user.py 987654321 user2 "Петр" "Петров"

# Добавляем третьего (только ID)
python3 add_user.py 555555555
```

## Проверка доступа

После добавления пользователя:
1. Пользователь открывает вашего бота в Telegram
2. Нажимает /start
3. Если все правильно - увидит кнопку "🛍 Открыть магазин"
4. Если нет - увидит сообщение "🔒 Доступ ограничен"

## Структура таблицы users

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    telegram_id INTEGER UNIQUE NOT NULL,  -- Обязательно
    username VARCHAR,                      -- Опционально
    first_name VARCHAR,                    -- Опционально
    last_name VARCHAR,                     -- Опционально
    is_active BOOLEAN DEFAULT TRUE,        -- Активен ли пользователь
    created_at TIMESTAMP,                  -- Дата создания
    
    -- Поля для автозаполнения заказов
    saved_full_name VARCHAR,
    saved_phone VARCHAR,
    saved_delivery_address TEXT,
    saved_city VARCHAR,
    saved_europost_office VARCHAR,
    saved_delivery_type VARCHAR
);
```

## Troubleshooting

**Ошибка: "Пользователь уже существует"**
- Пользователь уже добавлен в базу
- Скрипт предложит активировать его если он неактивен

**Ошибка: "Telegram ID должен быть числом"**
- Убедись что вводишь только цифры
- Telegram ID выглядит примерно так: 123456789

**Пользователь не может зайти в бот**
- Проверь что Telegram ID правильный
- Проверь что `is_active = True`
- Проверь что backend запущен и доступен
