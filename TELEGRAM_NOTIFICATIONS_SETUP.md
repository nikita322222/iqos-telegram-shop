# Настройка уведомлений о заказах в Telegram

## Что сделано

✅ Добавлена система уведомлений о заказах в Telegram группу
✅ Кнопки для принятия/отклонения заказов
✅ Автоматическое обновление статуса заказа
✅ Webhook сервер для приема уведомлений от backend

## Как настроить

### 1. Создайте Telegram группу для заказов

1. Создайте новую группу в Telegram
2. Добавьте вашего бота в эту группу
3. Сделайте бота администратором группы

### 2. Получите ID группы

Есть несколько способов:

**Способ 1: Через @userinfobot**
1. Добавьте бота @userinfobot в вашу группу
2. Он отправит ID группы (будет отрицательное число, например `-1001234567890`)
3. Удалите @userinfobot из группы

**Способ 2: Через API**
1. Отправьте любое сообщение в группу
2. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_BOT_TOKEN>/getUpdates`
3. Найдите `"chat":{"id":-1001234567890}` - это ID вашей группы

### 3. Добавьте переменные окружения

**Локально (bot/.env):**
```env
BOT_TOKEN=ваш_токен
BACKEND_URL=https://iqos-backend.onrender.com
MINI_APP_URL=https://iqos-shop.vercel.app
ADMIN_GROUP_ID=-1001234567890
```

**На сервере (если бот будет на сервере):**
Добавьте `ADMIN_GROUP_ID` в переменные окружения

**Backend (.env или Render):**
```env
BOT_WEBHOOK_URL=http://localhost:8001/webhook/order
```

Если бот на сервере, укажите его URL:
```env
BOT_WEBHOOK_URL=https://ваш-бот-сервер.com/webhook/order
```

### 4. Установите зависимости

```bash
cd backend
pip install -r requirements.txt

cd ../bot
pip install -r requirements.txt
```

### 5. Запустите бота

```bash
cd bot
python3 main.py
```

Бот теперь запускает:
- Polling для обработки команд пользователей
- Webhook сервер на порту 8001 для приема уведомлений о заказах

## Как это работает

1. **Пользователь оформляет заказ** в Mini App
2. **Backend создает заказ** в базе данных
3. **Backend отправляет webhook** на бота (http://localhost:8001/webhook/order)
4. **Бот получает данные** и отправляет сообщение в группу
5. **Админ видит заказ** с кнопками "Принять" и "Отклонить"
6. **При нажатии кнопки** бот обновляет статус заказа через API backend

## Формат уведомления

```
🔔 НОВЫЙ ЗАКАЗ

📋 Заказ №123
👤 Клиент: Иванов Иван Иванович
📱 Телефон: +375 29 123-45-67
💰 Сумма: 150.00 BYN
💳 Оплата: Наличные

🚚 Доставка по Минску
📍 Адрес: ул. Примерная, д. 1, кв. 1
🕐 Время: 13:00-17:00
📅 Дата: 2026-01-15

📦 Товары:
  • Purple Wave x2 = 50.00 BYN
  • Turquoise Selection x1 = 25.00 BYN

[✅ Принять заказ] [❌ Отклонить заказ]
```

## Статусы заказов

- `pending` - Ожидает подтверждения (по умолчанию)
- `confirmed` - Принят администратором
- `completed` - Выполнен
- `cancelled` - Отклонен

## Тестирование

1. Запустите backend и бота
2. Откройте Mini App в Telegram
3. Добавьте товары в корзину
4. Оформите заказ
5. Проверьте группу - должно прийти уведомление
6. Нажмите кнопку "Принять" или "Отклонить"
7. Статус заказа обновится в базе данных

## Troubleshooting

**Уведомления не приходят:**
- Проверьте что бот добавлен в группу и является администратором
- Проверьте что `ADMIN_GROUP_ID` правильный (должен быть отрицательным числом)
- Проверьте что webhook сервер запущен (порт 8001)
- Проверьте логи бота

**Кнопки не работают:**
- Проверьте что backend доступен для бота
- Проверьте что `BACKEND_URL` правильный в bot/.env
- Проверьте логи бота при нажатии кнопки

**Backend не может отправить webhook:**
- Если backend на Render, а бот локально - webhook не дойдет
- Используйте ngrok для локальной разработки или разверните бота на сервере
