"""
Скрипт для обновления схемы БД на продакшн сервере
ВНИМАНИЕ: Это удалит все данные на продакшн!
"""
import requests

PRODUCTION_URL = "https://iqos-backend.onrender.com"

def trigger_migration():
    """Триггерит пересоздание БД на продакшн через перезапуск"""
    print("⚠️  ВНИМАНИЕ: Это действие удалит все данные на продакшн сервере!")
    print(f"🔄 Сервер: {PRODUCTION_URL}")
    
    response = input("\nПродолжить? (yes/no): ")
    if response.lower() != 'yes':
        print("❌ Отменено")
        return
    
    print("\n📝 Инструкция по обновлению продакшн:")
    print("1. Зайдите в панель Render.com")
    print("2. Найдите ваш сервис iqos-backend")
    print("3. Нажмите 'Manual Deploy' -> 'Clear build cache & deploy'")
    print("4. После деплоя запустите: python3 upload_to_production.py")
    print("\nИли просто сделайте git push - Render автоматически задеплоит изменения")

if __name__ == "__main__":
    trigger_migration()
