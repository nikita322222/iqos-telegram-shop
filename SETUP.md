# 🚀 Инструкция по запуску IQOS Telegram Mini App

## Этап 1: Подготовка окружения

### Требования
- Python 3.11+
- Node.js 18+
- pip и npm

## Этап 2: Настройка Backend

```bash
cd backend

# Установка зависимостей
pip install -r requirements.txt

# Инициализация базы данных с тестовыми данными
python init_data.py
```

**ВАЖНО:** Откройте файл `backend/init_data.py` и добавьте ваш Telegram ID в список `test_users`:

```python
test_users = [
    {
        "telegram_id": YOUR_TELEGRAM_ID,  # Замените на ваш ID
        "username": "your_username",
        "first_name": "Ваше",
        "last_name": "Имя"
    }
]
```

Чтобы узнать свой Telegram ID, напишите боту [@userinfobot](https://t.me/userinfobot)

После добавления ID запустите снова:
```bash
python init_data.py
```

## Этап 3: Запуск Backend

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на `http://localhost:8000`

## Этап 4: Настройка Frontend

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## Этап 5: Запуск Telegram Bot

```bash
cd bot

# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python main.py
```

## Этап 6: Настройка Telegram Bot

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Найдите вашего бота (токен: `8572291552:AAHtF7dmGpGd4ZCn5TvKuweESyeOogFBv9s`)
3. Настройте Mini App:
   - Отправьте команду `/mybots`
   - Выберите вашего бота
   - Выберите "Bot Settings" → "Menu Button"
   - Выберите "Configure menu button"
   - Введите URL: `http://localhost:5173` (для разработки)
   - Введите текст кнопки: "Открыть магазин"

## Этап 7: Тестирование

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Если ваш Telegram ID добавлен в базу, вы увидите кнопку "Открыть магазин"
4. Нажмите на кнопку - откроется Mini App

## Для продакшена

### Backend
1. Смените `SECRET_KEY` в `backend/.env`
2. Используйте PostgreSQL вместо SQLite
3. Настройте HTTPS
4. Обновите `CORS_ORIGINS`

### Frontend
1. Соберите проект: `npm run build`
2. Разместите на хостинге с HTTPS
3. Обновите `VITE_API_URL` на продакшн URL

### Bot
1. Обновите `MINI_APP_URL` в `bot/.env` на продакшн URL
2. Добавьте `ADMIN_TELEGRAM_ID` для получения уведомлений о заказах
3. Запустите бота через systemd или supervisor

## Структура проекта

```
├── bot/              # Telegram Bot (aiogram)
│   ├── main.py       # Основной файл бота
│   ├── config.py     # Конфигурация
│   └── .env          # Переменные окружения
│
├── backend/          # FastAPI Backend
│   ├── main.py       # API endpoints
│   ├── models.py     # SQLAlchemy модели
│   ├── schemas.py    # Pydantic схемы
│   ├── auth.py       # Аутентификация Telegram
│   ├── database.py   # Настройка БД
│   ├── init_data.py  # Инициализация данных
│   └── .env          # Переменные окружения
│
└── frontend/         # React Mini App
    ├── src/
    │   ├── pages/    # Страницы приложения
    │   ├── components/ # Компоненты
    │   ├── context/  # React Context (корзина)
    │   ├── api/      # API клиент
    │   └── App.jsx   # Главный компонент
    └── .env          # Переменные окружения
```

## Добавление новых пользователей

Есть два способа:

### 1. Через базу данных напрямую
```python
from backend.database import SessionLocal
from backend.models import User

db = SessionLocal()
user = User(
    telegram_id=123456789,
    username="new_user",
    first_name="Имя",
    last_name="Фамилия"
)
db.add(user)
db.commit()
```

### 2. Через init_data.py
Добавьте пользователя в список `test_users` и запустите скрипт снова.

## Добавление товаров

Аналогично пользователям - через `init_data.py` или напрямую в БД.

## Troubleshooting

### Бот не отвечает
- Проверьте, что бот запущен (`python bot/main.py`)
- Проверьте токен в `bot/.env`
- Проверьте, что backend запущен

### Mini App не открывается
- Проверьте URL в настройках бота
- Убедитесь, что frontend запущен
- Проверьте консоль браузера на ошибки

### Ошибка "Доступ ограничен"
- Убедитесь, что ваш Telegram ID добавлен в базу
- Проверьте, что backend запущен и доступен
- Проверьте логи бота

### Ошибки CORS
- Убедитесь, что URL frontend добавлен в `CORS_ORIGINS` в `backend/.env`

## Полезные команды

```bash
# Просмотр логов бота
cd bot && python main.py

# Просмотр логов backend
cd backend && uvicorn main:app --reload --log-level debug

# Пересоздание БД
cd backend && rm iqos_shop.db && python init_data.py

# Сборка frontend для продакшена
cd frontend && npm run build
```
