# 🤖 Деплой Telegram бота на сервер

## Проблема

Сейчас бот работает только когда запущен на вашем компьютере. Когда компьютер выключен - бот не работает и не отправляет уведомления.

## Решение

Нужно разместить бота на сервере, который работает 24/7.

---

## 🎯 Варианты размещения бота

### Вариант 1: Render.com (РЕКОМЕНДУЕТСЯ) ⭐

**Плюсы:**
- ✅ Бесплатный тариф
- ✅ Простой деплой из GitHub
- ✅ Автоматические перезапуски
- ✅ Логи и мониторинг
- ✅ У вас уже есть аккаунт (там backend)

**Минусы:**
- ⚠️ Засыпает после 15 минут неактивности (но просыпается при запросе)
- ⚠️ 750 часов в месяц бесплатно

**Стоимость:** Бесплатно

---

### Вариант 2: Railway.app

**Плюсы:**
- ✅ Простой деплой
- ✅ $5 бесплатно каждый месяц
- ✅ Не засыпает
- ✅ Хорошая документация

**Минусы:**
- ⚠️ Требуется привязка карты

**Стоимость:** $5/месяц бесплатно, потом ~$5-10/месяц

---

### Вариант 3: VPS (DigitalOcean, Hetzner, Contabo)

**Плюсы:**
- ✅ Полный контроль
- ✅ Можно разместить несколько проектов
- ✅ Не засыпает
- ✅ Высокая производительность

**Минусы:**
- ⚠️ Требует настройки Linux
- ⚠️ Нужно настраивать безопасность
- ⚠️ Платно с первого дня

**Стоимость:** $4-6/месяц

---

### Вариант 4: PythonAnywhere

**Плюсы:**
- ✅ Специализируется на Python
- ✅ Простая настройка
- ✅ Бесплатный тариф

**Минусы:**
- ⚠️ Ограничения на бесплатном тарифе
- ⚠️ Нет автоматического деплоя

**Стоимость:** Бесплатно / $5/месяц

---

## 🚀 Инструкция: Деплой на Render.com (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Подготовка кода

1. Создайте файл `bot/requirements.txt` (если его нет):

```txt
aiogram==3.4.1
aiohttp==3.9.1
python-dotenv==1.0.0
```

2. Создайте файл `bot/render.yaml`:

```yaml
services:
  - type: web
    name: iqos-shop-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_GROUP_ID
        sync: false
      - key: BACKEND_URL
        value: https://iqos-shop-backend.onrender.com
```

3. Убедитесь что в `bot/main.py` используются переменные окружения:

```python
import os
from dotenv import load_dotenv

load_dotenv()

BOT_TOKEN = os.getenv('BOT_TOKEN')
ADMIN_GROUP_ID = int(os.getenv('ADMIN_GROUP_ID', '-1003553312128'))
BACKEND_URL = os.getenv('BACKEND_URL', 'http://localhost:8000')
```

### Шаг 2: Загрузка на GitHub

```bash
cd bot
git add .
git commit -m "Prepare bot for deployment"
git push origin main
```

### Шаг 3: Создание сервиса на Render

1. Зайдите на https://render.com
2. Нажмите **"New +"** → **"Background Worker"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: `iqos-shop-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
   - **Root Directory**: `bot` (если бот в папке bot)

5. Добавьте переменные окружения:
   - `BOT_TOKEN` = ваш токен бота
   - `ADMIN_GROUP_ID` = `-1003553312128`
   - `BACKEND_URL` = `https://iqos-shop-backend.onrender.com`

6. Нажмите **"Create Background Worker"**

### Шаг 4: Проверка

1. Откройте логи на Render
2. Должно быть сообщение: "✅ Бот запущен!"
3. Напишите боту `/start` - должен ответить

---

## 🚀 Инструкция: Деплой на Railway.app

### Шаг 1: Подготовка

Те же файлы что и для Render (requirements.txt)

### Шаг 2: Деплой

1. Зайдите на https://railway.app
2. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий
4. Railway автоматически определит Python проект

### Шаг 3: Настройка

1. Откройте проект
2. Перейдите в **Variables**
3. Добавьте:
   - `BOT_TOKEN`
   - `ADMIN_GROUP_ID`
   - `BACKEND_URL`

4. В **Settings** → **Start Command** укажите:
   ```
   python bot/main.py
   ```

5. Сохраните и перезапустите

---

## 🚀 Инструкция: Деплой на VPS (для опытных)

### Шаг 1: Аренда VPS

Рекомендуемые провайдеры:
- **Hetzner** (Германия) - от €4/месяц
- **DigitalOcean** (США) - от $6/месяц
- **Contabo** (Германия) - от €4/месяц

### Шаг 2: Подключение к серверу

```bash
ssh root@your-server-ip
```

### Шаг 3: Установка зависимостей

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и pip
apt install python3 python3-pip git -y

# Установка supervisor (для автозапуска)
apt install supervisor -y
```

### Шаг 4: Клонирование проекта

```bash
cd /opt
git clone https://github.com/your-username/your-repo.git
cd your-repo/bot
```

### Шаг 5: Установка зависимостей Python

```bash
pip3 install -r requirements.txt
```

### Шаг 6: Создание .env файла

```bash
nano .env
```

Добавьте:
```
BOT_TOKEN=your_bot_token
ADMIN_GROUP_ID=-1003553312128
BACKEND_URL=https://iqos-shop-backend.onrender.com
```

### Шаг 7: Настройка автозапуска (Supervisor)

```bash
nano /etc/supervisor/conf.d/iqos-bot.conf
```

Добавьте:
```ini
[program:iqos-bot]
command=/usr/bin/python3 /opt/your-repo/bot/main.py
directory=/opt/your-repo/bot
user=root
autostart=true
autorestart=true
stderr_logfile=/var/log/iqos-bot.err.log
stdout_logfile=/var/log/iqos-bot.out.log
```

### Шаг 8: Запуск

```bash
supervisorctl reread
supervisorctl update
supervisorctl start iqos-bot
```

### Шаг 9: Проверка статуса

```bash
supervisorctl status iqos-bot
```

### Шаг 10: Просмотр логов

```bash
tail -f /var/log/iqos-bot.out.log
```

---

## 🔄 Автоматическое обновление бота

### На Render/Railway:

Бот обновляется автоматически при push в GitHub:

```bash
git add .
git commit -m "Update bot"
git push origin main
```

### На VPS:

Создайте скрипт обновления:

```bash
nano /opt/update-bot.sh
```

```bash
#!/bin/bash
cd /opt/your-repo
git pull origin main
cd bot
pip3 install -r requirements.txt
supervisorctl restart iqos-bot
```

Сделайте исполняемым:
```bash
chmod +x /opt/update-bot.sh
```

Обновление:
```bash
/opt/update-bot.sh
```

---

## 📊 Мониторинг бота

### Проверка работы:

1. **Render/Railway**: Смотрите логи в веб-интерфейсе
2. **VPS**: `supervisorctl status iqos-bot`

### Логи:

1. **Render/Railway**: Вкладка "Logs"
2. **VPS**: `tail -f /var/log/iqos-bot.out.log`

### Перезапуск:

1. **Render/Railway**: Кнопка "Manual Deploy" или "Restart"
2. **VPS**: `supervisorctl restart iqos-bot`

---

## ⚠️ Важные моменты

### 1. Webhook vs Polling

Сейчас бот использует **polling** (постоянно опрашивает Telegram).

Для production лучше использовать **webhook** (Telegram сам отправляет обновления):

```python
# В main.py замените:
# await dp.start_polling(bot)

# На:
from aiohttp import web

async def webhook_handler(request):
    update = await request.json()
    await dp.feed_update(bot, Update(**update))
    return web.Response()

app = web.Application()
app.router.add_post('/webhook', webhook_handler)

# Установка webhook
await bot.set_webhook(f"https://your-bot-url.com/webhook")

# Запуск сервера
web.run_app(app, host='0.0.0.0', port=8001)
```

### 2. Переменные окружения

Никогда не коммитьте `.env` файл в Git!

Добавьте в `.gitignore`:
```
.env
*.env
```

### 3. Безопасность на VPS

```bash
# Создайте отдельного пользователя
adduser botuser

# Запускайте бота от этого пользователя
# В supervisor config: user=botuser

# Настройте firewall
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable
```

---

## 🎯 Рекомендация

Для вашего проекта рекомендую **Render.com**:

1. ✅ Бесплатно
2. ✅ Простой деплой (5 минут)
3. ✅ Автоматические обновления из GitHub
4. ✅ Логи и мониторинг
5. ✅ У вас уже есть аккаунт

**Единственный минус**: бот засыпает после 15 минут неактивности, но просыпается при первом запросе (задержка ~30 секунд).

Если нужна 100% доступность 24/7 - используйте **Railway** ($5/месяц) или **VPS** ($4-6/месяц).

---

## 📞 Поддержка

Если возникнут проблемы при деплое:

1. Проверьте логи
2. Убедитесь что все переменные окружения установлены
3. Проверьте что токен бота правильный
4. Убедитесь что бот добавлен в админ группу

---

## ✅ Чеклист деплоя

- [ ] Создан `requirements.txt`
- [ ] Код загружен на GitHub
- [ ] Создан сервис на Render/Railway/VPS
- [ ] Добавлены переменные окружения
- [ ] Бот запущен (проверить логи)
- [ ] Протестирован `/start`
- [ ] Протестированы уведомления о заказах
- [ ] Настроен автоматический деплой

После выполнения всех шагов бот будет работать 24/7 даже когда ваш компьютер выключен! 🎉
